# Database Handoff Contract

Runtime DB: `apps/database/app.db`

Seed data: sample user, team, and task inserted by `apps/database/init_db.py --init`.

Verification: initialization and basic verification queries executed; see `artifacts/database/quality-report.md` and `artifacts/database/openlog.md` for details.
